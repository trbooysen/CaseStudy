trigger:
- master

pool:
  vmImage: 'Ubuntu-latest'

variables:
  dockerId: trbooysen
  imageName: casestudy.service.a

steps:
- script: 
  docker build DockerfileA -t $(dockerId)/$(imageName)
  docker login --username "$(dockerId)" --password "$(dockerPassword)"
  docker push $(dockerId)/$(imageName)
