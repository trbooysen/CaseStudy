trigger:
- master

pool:
  vmImage: 'Ubuntu-latest'

variables:
  imageName: 'dockerimageA'

steps:
- task: Docker@2
  displayName: Build an image
  inputs:
    repository: $(imageName)
    command: build
    Dockerfile: DockerfileA
