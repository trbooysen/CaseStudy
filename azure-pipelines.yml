name : Docker-ServiceA-CI
trigger:
  branches:
    include:
      - master

pool:
  vmImage: 'ubuntu-latest'

variables:
  ImageName: 'trbooysen/servicea:$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:  
  - job: Build
    displayName: Build and push Docker image
    steps:
    - task: Docker@2
      inputs:
        containerRegistry: 'Docker Hub'
        repository: 'casestudy.service.a'
        command: 'build'
        Dockerfile: 'DockerfileA'

        imageName: '$(ImageName)'